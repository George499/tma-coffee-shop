{
  "name": "tma-coffee-shop",
  "version": "0.0.0",
  "private": true,
  "description": "Telegram Mini App: coffee shop catalog and cart with order delivery to admin chat.",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck"
  }
}
